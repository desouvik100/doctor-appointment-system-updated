# Frontend Environment Variables

# API Configuration
REACT_APP_API_URL=http://localhost:5000

# Payment Mode Configuration
# Set to 'true' to enable real Stripe payments, 'false' for test mode (no payments)
REACT_APP_USE_STRIPE_PAYMENTS=false

# Stripe Configuration (only used when REACT_APP_USE_STRIPE_PAYMENTS=true)
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_key_here

# Google OAuth Configuration (for Google Sign-In)
# Get your Client ID from Google Cloud Console: https://console.cloud.google.com/
# 1. Create a project or select existing one
# 2. Go to APIs & Services > Credentials
# 3. Create OAuth 2.0 Client ID (Web application)
# 4. Add authorized JavaScript origins: http://localhost:3001
# 5. Copy the Client ID here
REACT_APP_GOOGLE_CLIENT_ID=your_google_client_id_here

# Build Configuration
GENERATE_SOURCEMAP=false
CI=false
DISABLE_ESLINT_PLUGIN=true

# Environment
NODE_ENV=development